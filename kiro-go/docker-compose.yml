services:
  kiro-go:
    build: .
    network_mode: host
    volumes:
      - ./data:/app/data
    environment:
      - CONFIG_PATH=/app/data/config.json
      - VPN_PROXY_URL=http://127.0.0.1:7890
      - PORT=8990
      - HOST=0.0.0.0
    restart: unless-stopped
